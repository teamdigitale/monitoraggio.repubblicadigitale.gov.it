{
  "type": "object",
  "properties": {
    "domanda testuale": { "type": "string" },
    "domanda numerica": { "type": "integer" },
    "domanda calendario": { "type": "string", "format": "date" },
    "domanda select": { "type": "string", "enum": ["opzione 1", "opzione 2"] },
    "domanda checkbox": {
      "type": "object",
      "properties": {
        "check 1": { "type": "boolean" },
        "check 2": { "type": "boolean" }
      }
    },
    "domanda rating": { "type": "integer", "minimum": 1, "maximum": 5 }
  },
  "required": [
    "domanda testuale",
    "domanda numerica",
    "domanda calendario",
    "domanda select",
    "domanda checkbox",
    "domanda rating"
  ]
}
