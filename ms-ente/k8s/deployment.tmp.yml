---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-ente-deployment
  labels:
    app: ms-ente
spec:
  selector:
    matchLabels:
        app: ms-ente
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: ms-ente
    spec:
      containers:
        - name: ms-ente
          image: ${ECR_REPOSITORY}:${IMAGE_TAG}
          ports:
            - containerPort: 80
              protocol: TCP
          imagePullPolicy: Always
