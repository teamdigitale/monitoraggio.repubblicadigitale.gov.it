package it.pa.repdgt.surveymgmt.service;

import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
public class RuoloServiceTest {

	/*
	 * @Mock
	 * private RuoloRepository ruoloRepository;
	 * 
	 * @Autowired
	 * 
	 * @InjectMocks
	 * private RuoloService ruoloService;
	 * 
	 * RuoloEntity ruolo;
	 * List<RuoloEntity> listaRuoli;
	 * 
	 * @BeforeEach
	 * public void setUp() {
	 * ruolo = new RuoloEntity();
	 * ruolo.setCodice("RUOLO");
	 * listaRuoli = new ArrayList<>();
	 * listaRuoli.add(ruolo);
	 * }
	 * 
	 * @Test
	 * public void getRuoliByCodiceFiscaleTest() {
	 * when(this.ruoloRepository.findRuoliByCodiceFiscale("CODICEFISCALE")).
	 * thenReturn(listaRuoli);
	 * List<RuoloEntity> risultato =
	 * ruoloService.getRuoliByCodiceFiscale("CODICEFISCALE");
	 * assertThat(risultato.size()).isEqualTo(listaRuoli.size());
	 * }
	 */
}
