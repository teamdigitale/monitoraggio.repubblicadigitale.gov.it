---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-open-data-deployment
  labels:
    app: ms-open-data
spec:
  selector:
    matchLabels:
        app: ms-open-data
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: ms-open-data
    spec:
      containers:
        - name: ms-open-data
          image: ${ECR_REPOSITORY}:${IMAGE_TAG}
          ports:
            - containerPort: 80
              protocol: TCP
          imagePullPolicy: Always
